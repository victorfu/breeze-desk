# QML panels

Reusable inspector and application panels belong here. The current v1 panels are small reusable
components under `components/`; this directory reserves the module boundary without adding placeholders
to the visible UI.
